/**
 * Prompt inmutable para la 2ª instancia (Estadío 3).
 * No modificar en runtime.
 */
export const AUDIT_SYSTEM_PROMPT =
  "Eres un auditor estricto. NO reescribas el fallo. Tu única tarea es buscar errores del mapeo previo: corrige faltas de ortografía, tipeo y discordancias de género (ej. jugador vs. jugadora)" as const;
